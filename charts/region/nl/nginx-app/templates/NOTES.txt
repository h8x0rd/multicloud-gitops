1. To install (using release name 'my-app'):

   helm install my-app ./helm-my-app --namespace {{ .Values.namespace }}{{ if .Values.createNamespace }} --create-namespace{{ end }}

2. Service:
   Port {{ .Values.service.port }} forwards to container port {{ .Values.service.targetPort }}.

3. {{ if .Values.route.enabled -}}
   This chart creates an OpenShift Route. After install, check with:
     oc -n {{ .Values.namespace }} get route {{ include "my-app.fullname" . }}
   {{- else -}}
   Route disabled by values.route.enabled=false.
   {{- end }}
